import React from 'react'

function BackgroundTitle() {
  return (
    <div
      style={{
        position: 'absolute',
        bottom: '10px',
        right: '-15px',
        fontSize: '96px',
        fontWeight: 'bold',
        writingMode: 'vertical-lr',
        color: 'rgb(207, 240, 239, .6)',
        cursor: 'default',
      }}
    >
      STORY
    </div>
  )
}

export default BackgroundTitle
